function lowerToUppers(ch) {
    if (ch == ch.toUpperCase()) {
        console.log("upper-case");
    } else if (ch == ch.toLowerCase()) {
        console.log("lower-case");
    }
}

lowerToUppers('L');